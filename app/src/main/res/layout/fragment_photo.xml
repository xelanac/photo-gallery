<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.PhotoFragment"
    android:background="@color/white">

   <LinearLayout
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:orientation="vertical">
      <ImageView
          android:id="@+id/photo_view"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:layout_centerInParent="true"
          android:paddingTop="@dimen/padding_top_bottom_photo"
          android:paddingBottom="@dimen/padding_top_bottom_photo"
          android:paddingStart="@dimen/padding_start_end_photo"
          android:paddingEnd="@dimen/padding_start_end_photo"/>
      <RelativeLayout
          android:layout_width="match_parent"
          android:layout_height="wrap_content">
         <Button
             android:id="@+id/deleteButton"
             android:layout_width="@dimen/botton_width"
             android:layout_height="wrap_content"
             android:text="@string/delete"
             android:layout_alignParentStart="true"
             android:layout_marginStart="@dimen/padding_start_end_photo"
             android:backgroundTint="@color/red"/>
         <Button
             android:id="@+id/saveButton"
             android:layout_width="@dimen/botton_width"
             android:layout_height="wrap_content"
             android:text="@string/save"
             android:layout_alignParentEnd="true"
             android:layout_marginEnd="@dimen/padding_start_end_photo"
             android:backgroundTint="@color/green"/>
      </RelativeLayout>
   </LinearLayout>
</RelativeLayout>